/**
 * 将rpx转换为px（Web/通用环境）
 * @param rpx 需要转换的rpx值
 * @param customBaseWidth 自定义基准宽度，默认为设计稿宽度
 * @returns 计算出的px值
 */
export declare function rpxToPx(window: any, rpx: number, customBaseWidth?: number): number;
/**
 * 将CSS字符串中的rpx单位转换为px单位
 * @param css 需要转换的CSS字符串
 * @returns 转换后的CSS字符串
 */
export declare function convertCssRpx(window: any, css: string): string;
