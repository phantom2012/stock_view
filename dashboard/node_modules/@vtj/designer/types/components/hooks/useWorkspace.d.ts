import { ComputedRef, Ref } from 'vue';
import { ActionMenuItem, TabsItem } from '@vtj/ui';
import { Widget } from '../../framework';
export declare function useWorkspace(widgets: ComputedRef<Widget[]>): {
    menus: ComputedRef<{
        name: string;
        command: string;
        label: string;
        checked: boolean;
    }[]>;
    tabs: ComputedRef<TabsItem[]>;
    activeFileId: Ref<string, string>;
    menuChecked: Ref<string, string>;
    onCloseTab: (name: string) => void;
    onMenuChecked: (item: ActionMenuItem) => void;
    closeAllTabs: () => void;
    closeOtherTabs: () => void;
};
