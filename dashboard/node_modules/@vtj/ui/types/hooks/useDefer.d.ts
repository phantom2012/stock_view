/**
 * 延时渲染
 * @param maxFrameCount
 * @returns
 *
 * @example
 * const defer = useDefer();
 * v-if="defer(10)" // 在第10帧开始渲染
 *
 */
export declare function useDefer(maxFrameCount?: number): (showInFrameCount: number) => boolean;
